## 1. node.js installation


## 2. vs code development setup

- created backend name folder
- open with vs code
- npm -v check in terminal
- node -v


## 3. backend creation with node and express js

- npm init
- above command create package.json and it contains all dependencies for backend which we will create
- created index.js file which will be the main entry point 

## 4. How to run backend or normal JS file with help of node

- difference between node and nodemon
        to run normal file we need node.js and to see continjues chages we installed nodemon
        with help of command npm i nodemon -g
- we run code for normal JS file : node filename or filepath
- so we run backend : 
        
       - we write a script in package.
       - we wrote teo scripts one for dev one for starts in package.json file
       - to run the scripts we go to main folder in terminal where package.json exists npm run --scriptkey--

## 5. installation of express.js for creating backend server

- npm i express 
- above command installs express for backend server dependencies we can check it in package.json
- after installation we have express module server.js which is main entry point 


we send request to server using send method
we 

## 6.
